 {% extends "./subviews/printanswers/printanswers_question.twig" %}
 {% block question_answer %}
 <div class="fr-col-lg-8">
    <div class="fr-grid-row">
        <div class="fr-col-lg-4 text-end">
        </div>
        <div class="fr-col-lg-8 text-start">
            {% if question.answervalue is same as("M") %}
                {{ gT("Male") }} <i class="fa fa-mars"></i>
            {% elseif question.answervalue is same as("F") %}
                {{ gT("Female") }} <i class="fa fa-venus"></i>
            {% else %}
                {{question.answervalue}}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}

